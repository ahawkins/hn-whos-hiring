<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Job postings from Hackernews hiring threads">
    <meta name="author" content="Adam Hawkins">

    <title>Who's Hiring on HackerNews</title>

    <!-- Custom styles for this template -->
    <link href="style.css" rel="stylesheet">
  </head>
	<body>
		<p>
			RSS feed of Hacker News Who's Hiring Threads. Made by <a href="https://twitter.com/adman65">Adam Hawkins</a>. <a href="https://github.com/ahawkins/whos-hiring">Source</a>.
		</p>

		<p class="filters">
			Show:
			<a id="remote-only" <%= remote_only? ? 'class="is-active"' : nil %> href="/?remote=true">Remote Jobs</a>
			<a id="all-jobs" <%= all_jobs? ? 'class="is-active"' : nil %> href="/">All Jobs</a>
		</p>

		<p><a href="<%= rss_url %>">RSS Feed</a></p>

		<% @jobs.each do |job| %>
			<li class="job">
				<date><%= job.timestamp.strftime("%Y-%m-%d") %></date> - <a id="job-<%= job.id %>" href="<%= job.link %>"><%=h job.to_s %></a>
			</li>
		<% end %>
	</body>
</html>
